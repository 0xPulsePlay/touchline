{
  "name": "touchline",
  "version": "0.1.0",
  "private": true,
  "description": "Touchline — options on the probability path. One-touch markets on TxLINE de-margined win probabilities, priced by the p/B martingale bound and resolved by a Merkle proof of a single odds tick, verified against the Solana-anchored daily batch root.",
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"pnpm --filter @touchline/server dev\" \"pnpm --filter @touchline/web dev\"",
    "test": "pnpm --filter @touchline/server test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
